<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andrezamoreira.pontos">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_iblue"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_iblue_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.ResultEntriesRangeActivity"></activity>
        <activity android:name=".controllers.DateRangeActivity" />
        <activity android:name=".controllers.ModifyAPIKeyActivity" />
        <activity android:name=".controllers.ModifyPasswordActivity" />
        <activity android:name=".controllers.MainActivity" />
        <activity android:name=".controllers.RegisterActivity" />
        <activity android:name=".controllers.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>